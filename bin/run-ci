#!/bin/bash

set -e

# Script that replicates the tests being run on CI

echo "Running linters" && echo ""
bundle exec rake lint:all

echo "Running frontend tests" && echo ""
yarn test

echo "Running rails tests" && echo ""
bundle exec rake spec
